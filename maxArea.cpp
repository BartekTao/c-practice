#include <iostream>
#include <vector>
#include <map>

using namespace std;

int maxArea(vector<int>& height) {
        for(int i = 1; i < height.size(); i++){
            
        }
        return -1;
    }